# bf-493n: gap-analysis.md verification

## Task
Update gap-analysis.md to reflect current implementation status (v0.49.0).

## Finding
All 7 requested items were already addressed by prior commits before this bead ran:

1. **Compaction** (Future Improvements #7) — ✅ marked complete, full details in "Compaction/Multi-Window Context" section (v0.48.0)
2. **Prompt caching simulation** (Future Improvements #2) — ✅ marked complete; `cache.PromptCache` is fully wired in `internal/proxy/handler.go` (`checkPromptCache`, `tryStorePromptCache`, `SetPromptCache`)
3. **LiteLLM Backend table row** — ✅¹ with footnote clarifying CLASP routes to an external LiteLLM server; `internal/provider/litellm.go` is a fully implemented `Provider`
4. **MiniMax, Qwen, Grok in comparison table** — ✅ all three rows present with ✅ for CLASP
5. **Version numbers** — v0.49.0 referenced throughout; Dynamic Model Discovery still shows v0.44.11 (correct, that's when it was added)
6. **Stream timeout** — ✅ Fixed in v0.49.0 (bf-5puj) with auto-warn and setup wizard prompts
7. **Newer models section** — Sections present for o3, Gemini 2.5, and xAI Grok

## Commits that did the work
- `302d999` — update to reflect v0.49.0 implementation status
- `755e617` — update to reflect v0.49.0 implementation status
- `16aedb5` — fix CLASP_HTTP_TIMEOUT env var name and add LiteLLM footnote
