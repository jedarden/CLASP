# bf-5370: Unit tests for context_scaling.go

## Status: Already Complete

The unit tests for `internal/translator/context_scaling.go` were already added in commit 2646259.

## Existing Test Coverage

The file `internal/translator/context_scaling_test.go` contains comprehensive tests:

1. **TestGetModelContextWindow_KnownModels** - Tests direct lookup for known models (Claude, GPT-4o, O1/O3, GPT-5, Gemini, Grok, Qwen, DeepSeek, Ollama, OpenRouter)

2. **TestGetModelContextWindow_PrefixMatch** - Tests prefix matching for model variants (e.g., "claude-3-5-sonnet-20241022:preview")

3. **TestGetModelContextWindow_SuffixMatch** - Tests suffix matching for custom model names (e.g., "my-custom-gpt-4o")

4. **TestGetModelContextWindow_UnknownModel** - Tests default fallback to 128000 for unknown models

5. **TestNewContextScaler** - Tests ContextScaler creation with correct ratio calculation

6. **TestContextScaler_ScaleTokensForClaude** - Tests token scaling for various context sizes

7. **TestContextScaler_GetRealUsagePercent** - Tests percentage calculation

8. **TestContextScaler_ScaleUsage** - Tests combined input/output scaling

9. **TestContextScaler_ShouldScale** - Tests scaling logic

10. **TestContextScaler_GetContextInfo** - Tests ContextInfo struct population

11. **TestContextScaler_ScaleTokensForClaude_RatioBehavior** - Tests ratio >= 1.0 vs < 1.0 behavior

12. **TestGetModelContextWindow_EdgeCases** - Tests edge cases (colons, slashes, variants)

All tests pass successfully.
